from datetime import datetime


def take_screenshot() -> str:
    # Phase 1 does not include visual capture yet.
    return f"Screenshot capture is disabled in Phase 1 ({datetime.now().isoformat()})."